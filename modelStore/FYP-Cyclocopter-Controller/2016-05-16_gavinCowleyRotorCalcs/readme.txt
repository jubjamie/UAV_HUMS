CAVEATS:
(a) This is a horrible piece of code. It probably needs a rewrite from scratch now the approach is understood.
(b) There are likely mistakes hidden in here.
(c) Use as a guide only.

you probably want to run cyclocopter_power()
or you possibly want to edit then run findAlpha()
